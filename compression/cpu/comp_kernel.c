// Declare in and out vars

// 1. Implement pass through
// 2. Implement shift
// 3. Implment

void main() {
  uint last_p = 0;
  for (int i = 0; i < 1000; ++i) {
    uint p = img[i];
    if (p != last_p) {
      // Write RLE and color
    }
  }
}
