import rewards.art_of_rally

# run.out_dir = "out/aor_ppo_stacked/"
run.seed = 0
run.timesteps = 620000
run.n_stack = 4

# sb3_aor.PPO is a wrapper of stable_baselines3.PPO to enable config injection.
PPO.seed = 0
# Configured in the script's main
# PPO.n_steps = 10240
PPO.ent_coef = .01

rewards.art_of_rally.ArtOfRallyReward.reward_fn = @rewards.art_of_rally.TimeReward
rewards.art_of_rally.TimeReward.penalty_value = 1
